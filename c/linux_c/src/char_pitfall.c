/*
 * char fall
*/

#include <stdio.h>

int main(int argc, char** args) {

    char c;

    c = -1;

    if (c < 0) {
        printf("%d is minas number\n", c);
    }
    else {
        printf("%d is plus number\n", c);
    }

    return 0;
}
